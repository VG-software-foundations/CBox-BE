spring:
  liquibase:
    enabled: false
  datasource:
    username: postgres
    password: postgres
  jpa:
    properties.hibernate:
      batch_size: 50
      fetch_size: 50
      show_sql: true
      format_sql: true
      hbm2ddl.auto: validate
    open-in-view: false